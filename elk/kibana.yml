# Kibana가 사용할 호스트와 포트 설정
server.host: "0.0.0.0"
server.port: 5601       # 기본 포트 (필요에 따라 변경 가능)
elasticsearch.hosts: ["http://elasticsearch:9200"]

# SSL 설정 (HTTPS 활성화)
server.basePath: "/kibana"
server.rewriteBasePath: true

# SSL 설정 비활성화 (Nginx에서 처리하므로)
server.ssl.enabled: false
